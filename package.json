{
  "name": "PathWizard",
  "version": "0.0.1",
  "description": "A lightweight wrapper around `require` which finds modules and files based on the shortest unique path.",
  "main": "src/index.js",
  "keywords": [],
  "author": "@clocasto",
  "license": "MIT",
  "scripts": {
    "test": "mocha tests/index.js",
    "test-travis": "npm run transpile && istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- tests",
    "transpile": "babel index.js --out-file src/index.js --presets=es2015"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "chai": "^3.5.0",
    "chai-spies": "^0.7.1",
    "coveralls": "^2.11.15",
    "eslint": "^3.9.1",
    "eslint-config-airbnb": "^13.0.0",
    "fs-extra": "^1.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.1.2",
    "mocha-lcov-reporter": "^1.2.0"
  }
}
